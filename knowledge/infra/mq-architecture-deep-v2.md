# 消息队列架构深度解析

> 深入消息队列：RabbitMQ、Kafka、RocketMQ架构对比与选型。
> 源码级分析，包含生产环境最佳实践。
> 适用对象：后端工程师、架构师

---

## 1. 消息队列选型

### 1.1 核心能力对比

```
消息队列能力对比：

┌─────────────────────────────────────────────────────────────┐
│  特性              │ RabbitMQ │ Kafka    │ RocketMQ    │
│ ─────────────────┼──────────┼──────────┼─────────────┤
│ 吞吐量            │ 万级      │ 百万级    │ 十万级       │
│ 延迟              │ 微秒级    │ 毫秒级    │ 毫秒级       │
│ 可靠性            │ 高        │ 高        │ 高           │
│ 顺序性            │ 分区有序  │ 分区有序  │ 全局/分区有序 │
│ 消息积压          │ 差        │ 好        │ 好           │
│ 事务消息          │ ❌        │ ❌        │ ✅           │
│ 死信队列          │ ✅        │ ❌        │ ✅           │
│ 回溯消费          │ 差        │ 好        │ 好           │
│ 集群架构          │ 主从      │ 分区副本  │ 主从         │
│ 适用场景          │ 中小企业  │ 大数据    │ 金融电商     │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 可靠性保障

### 2.1 消息不丢方案

```
消息可靠性保障：

┌─────────────────────────────────────────────────────────────┐
│  生产者侧：                                                  │
│  ├── Confirm确认机制                                          │
│  ├── 事务消息                                                │
│  └── 本地消息表                                              │
│                                                             │
│  Broker侧：                                                  │
│  ├── 持久化存储                                              │
│  ├── 副本机制（ISR）                                         │
│  └── 主从同步                                                │
│                                                             │
│  消费者侧：                                                  │
│  ├── 手动ACK                                                │
│  ├── 消费重试                                                │
│  └── 死信队列                                                │
│                                                             │
│  最终一致性：                                                │
│  ├── 消息幂等性设计                                          │
│  ├── 业务补偿                                                │
│  └── 对账机制                                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 自测题

### 3.1 单选题

1. Kafka保证消息有序性的粒度是：
   A. 全局有序  B. 分区有序  C. Topic有序  D. 不保证有序
   答案：B

---

> 本文档适用对象：后端工程师、架构师
> 难度：资深专家级
