# ad-agent 依赖文件
# 解释器要求：Python 3.13（请使用仓库根目录的 make ad-agent-install）

# 核心依赖
requests>=2.28.0
pyjwt>=2.6.0
PyYAML>=6.0
fastapi>=0.100.0
uvicorn>=0.22.0
PyMySQL>=1.1.0
fastmcp>=3.4.0,<4.0.0

# 可选依赖（启用时使用）
# google-ads>=25.0.0  # Google Ads SDK（可选，默认使用 HTTP）
# google-auth>=2.0.0  # Google OAuth（DV360 使用 openssl 绕过）
# anthropic>=0.40.0  # Claude SDK（可选，skill-up claude_sdk Engine）

# 测试依赖
pytest>=7.0.0
pytest-cov>=4.0.0

# 日志增强（可选）
# structlog>=23.0.0
