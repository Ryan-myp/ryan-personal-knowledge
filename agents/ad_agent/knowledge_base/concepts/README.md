# Concepts

Concept pages contain distilled, reusable knowledge. The existing
`platforms/`, `business/` and `expertise/` directories are the canonical
domain storage for these pages; `wiki_type: concept` makes that logical role
explicit without forcing a risky path migration.
